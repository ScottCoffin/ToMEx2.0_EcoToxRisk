% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{make_all_pSSDs}
\alias{make_all_pSSDs}
\title{Run the PSSD++ workflow across tiers, environments, and ERMs}
\usage{
make_all_pSSDs(
  MC_sim_df = NULL,
  tiers = c(3),
  environments = c("Freshwater", "Marine"),
  erms = c("Food Dilution", "Tissue Translocation"),
  erm_registry = NULL,
  sim = 10,
  cv_uf = 0.5,
  rmore_method = "step",
  quantile_type = 8,
  debug_option = FALSE,
  parallel = TRUE,
  workers = max(1, parallel::detectCores() - 1),
  base_cache_dir = file.path(tempdir(), "pssd_cache"),
  base_output_path = file.path(tempdir(), "pssd_figures"),
  overwrite_cache = FALSE,
  progress = NULL
)
}
\arguments{
\item{MC_sim_df}{Monte Carlo alignment output (data frame).}

\item{tiers}{Numeric vector of threshold tiers to evaluate.}

\item{environments}{Character vector of environments to include.}

\item{erms}{Character vector of ERM names.}

\item{erm_registry}{Named list of ERM-specific data frames created from
the aligned data.}

\item{sim}{Number of PSSD simulations per combination.}

\item{cv_uf}{Coefficient of variation for uncertainty factors.}

\item{rmore_method}{Distribution method for sampling species-level NOEC
distributions in the PSSD++ step. \code{"step"} (default) uses a trapezoidal
piecewise-uniform approximation following Wigger et al. (2020), which is
consistent with published results. \code{"lognormal"} uses a log-normal
short-cut that is faster but assumes symmetric log-scale variability; use
only when speed matters more than strict comparability to the manuscript.}

\item{quantile_type}{Quantile type used when summarizing outputs.}

\item{debug_option}{Logical; saves intermediate objects for debugging.}

\item{parallel}{Logical; run combinations in parallel.}

\item{workers}{Number of workers when \code{parallel = TRUE}.}

\item{base_cache_dir}{Directory for cached PSSD objects.}

\item{base_output_path}{Directory for figures.}

\item{overwrite_cache}{Logical; recompute even if cache exists.}

\item{progress}{Logical; show progress bars. If NULL, disables in RStudio or
knitr.}
}
\value{
A named list of PSSD results (one per combination).
}
\description{
Generates probabilistic SSDs, summary statistics, and figures with caching
for each combination of tier, environment, and ERM.
}
\examples{
\dontrun{
  # After running MC_sim_align_parallel to get mc_out and building erm_registry:
  pSSDs <- make_all_pSSDs(
    MC_sim_df    = mc_out,
    tiers        = 3,
    environments = c("Freshwater", "Marine"),
    erms         = c("Food Dilution", "Tissue Translocation"),
    erm_registry = erm_registry,
    sim          = 3,
    parallel     = FALSE
  )
  names(pSSDs)
}
}
