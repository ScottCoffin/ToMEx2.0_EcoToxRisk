% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{MC_sim_align_parallel}
\alias{MC_sim_align_parallel}
\title{Monte Carlo alignments in parallel}
\usage{
MC_sim_align_parallel(
  tox_data = aoc_risk_paper,
  param_matrix = mat,
  x1D_set = 1,
  x2D_set = 5000,
  n_sim = 10,
  num_cores = 1,
  suppress_warnings = TRUE
)
}
\arguments{
\item{tox_data}{Data frame of toxicity records (defaults to the bundled dataset).}

\item{param_matrix}{Parameter matrix created by \link[=matrix_function]{matrix_function()}.}

\item{x1D_set}{Lower particle-size bound (um) for alignment.}

\item{x2D_set}{Upper particle-size bound (um) for alignment.}

\item{n_sim}{Number of alignment simulations to run.}

\item{num_cores}{Number of workers. Defaults to \code{1} (sequential), per CRAN
policy that packages must not default to using multiple cores. For large
jobs (\code{n_sim} in the hundreds or thousands), parallel processing is
strongly recommended for practical runtimes: set \code{num_cores = "auto"} to
use all but one available core, or supply an explicit integer.}

\item{suppress_warnings}{Logical; silence alignment warnings.}
}
\value{
A data frame with aligned doses for each simulation.
}
\description{
Runs the alignment workflow across Sobol-sampled parameter sets, returning
simulation-ready microplastic ecotoxicity data.
}
\examples{
\dontrun{
  # Use the bundled mini dataset for a fast test (3 simulations, 1 core)
  data(tomex2_mini)
  mat <- matrix_function(n_sobol = 3, params = param_default_values)
  mc_out <- MC_sim_align_parallel(
    tox_data   = tomex2_mini,
    param_matrix = mat,
    n_sim      = 3,
    num_cores  = 1
  )
  head(mc_out)

  # For larger jobs, parallel processing is strongly recommended:
  mc_out_parallel <- MC_sim_align_parallel(
    tox_data   = tomex2_mini,
    param_matrix = mat,
    n_sim      = 500,
    num_cores  = "auto"
  )
}
}
