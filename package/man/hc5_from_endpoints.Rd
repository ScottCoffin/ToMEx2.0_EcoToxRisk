% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference_api.R
\name{hc5_from_endpoints}
\alias{hc5_from_endpoints}
\title{Compute an HC distribution from per-species endpoints}
\usage{
hc5_from_endpoints(
  endpoints,
  sim = 1000,
  cv_dp = 0.15,
  cv_uf = 0.5,
  rmore_method = c("step", "lognormal"),
  hcx = 0.05,
  apply_assessment_factors = FALSE
)
}
\arguments{
\item{endpoints}{A data frame with columns \code{Species} and \code{ec}. \code{Species}
identifies species, and \code{ec} is a positive numeric effect concentration on
the linear scale, in particles/L. Optional column \code{ec_sd} supplies
endpoint-level standard deviations on the same scale for \code{DP.SD}.}

\item{sim}{Integer number of Monte Carlo iterations.}

\item{cv_dp}{Numeric coefficient of variation for endpoint uncertainty.}

\item{cv_uf}{Numeric coefficient of variation for uncertainty factors.}

\item{rmore_method}{Sampling method for species with three or more endpoints;
either \code{"step"} or \code{"lognormal"}.}

\item{hcx}{Numeric hazard concentration quantile, for example \code{0.05} for HC5.}

\item{apply_assessment_factors}{Logical. Defaults to \code{FALSE}, meaning inputs
are already NOEC-equivalent and assessment factors are treated as unit
multipliers.}
}
\value{
A list with \code{pSSD}, a species x \code{sim} matrix of sampled endpoint
concentrations, and \code{hc}, a numeric vector of HCx values, one per
simulation.
}
\description{
Thin, documented wrapper around the package's internal probabilistic-SSD
sampler, so externally generated, for example ML-predicted, per-species
effect concentrations can be run through the same engine as
\code{\link[=make_all_pSSDs]{make_all_pSSDs()}}.
}
\examples{
endpoints <- data.frame(
  Species = rep(c("sp_a", "sp_b", "sp_c"), each = 3),
  ec = c(20, 25, 40, 80, 95, 120, 300, 330, 380)
)
res <- hc5_from_endpoints(endpoints, sim = 50)
str(res)
}
