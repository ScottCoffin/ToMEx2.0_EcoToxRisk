% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssd_functions.R
\name{autoplot.tiered_SSD}
\alias{autoplot.tiered_SSD}
\title{ggplot2 autoplot method for tiered_SSD objects}
\usage{
\method{autoplot}{tiered_SSD}(
  object,
  x_label = "Concentration",
  erm_label = NULL,
  base_size = 12,
  point_size = 2,
  label_size = 3.5,
  ...
)
}
\arguments{
\item{object}{A \code{tiered_SSD} object returned by \code{\link[=make_tiered_SSDs]{make_tiered_SSDs()}}.}

\item{x_label}{Character string for the x-axis label (dose metric and
units, e.g. \code{"particles/L"}). Default \code{"Concentration"}.}

\item{erm_label}{Optional character string naming the ERM (e.g.
\code{"Food Dilution"}). Appended to the plot subtitle. Default \code{NULL}.}

\item{base_size}{Base font size (pt) passed to \code{theme_bw()}. Controls
overall text size and is the primary way to scale the plot for different
output dimensions. Default \code{12}.}

\item{point_size}{Size of the species data points. Default \code{2}.}

\item{label_size}{Size of the repelled species labels. Default \code{3.5}.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:theme]{ggplot2::theme()}}.}
}
\value{
A \code{ggplot} object. Returns \code{NULL} (with a warning) when fewer than 6
species were available and no SSD could be fit.
}
\description{
Produces a publication-ready Species Sensitivity Distribution plot from a
\code{tiered_SSD} object. The plot combines the model-averaged SSD curve and
confidence ribbon (\code{predictions}), empirical species points with repelled
labels (\code{collapsed_data}), and crosshair lines with a text annotation
pointing to the actual HCx position on the curve (\code{hc_data}).
}
\examples{
\dontrun{
result <- make_tiered_SSDs(fd_water, tier = 3)
ggplot2::autoplot(result, x_label = "particles/L", erm_label = "Food Dilution")
# Larger base size for a wider figure
ggplot2::autoplot(result, x_label = "particles/L", base_size = 14)
}

}
\seealso{
\code{\link[=plotly_ssd]{plotly_ssd()}} for an interactive version, \code{\link[=make_tiered_SSDs]{make_tiered_SSDs()}}
}
