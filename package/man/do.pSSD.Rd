% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/do_pssd.R
\name{do.pSSD}
\alias{do.pSSD}
\title{Probabilistic SSD generator (original implementation)}
\usage{
\method{do}{pSSD}(.data = NULL, ...)
}
\arguments{
\item{.data}{Matrix of endpoint values (rows = endpoints, cols = species). When
provided, it is treated as \code{DP}.}

\item{...}{Additional arguments used by the sampler. Expected arguments include
\code{DP}, \code{UFt}, \code{UFdd}, \code{SIM}, \code{CV.DP}, \code{CV.UF}, and
\code{apply_assessment_factors}. \code{UFt} and \code{UFdd} are optional assessment-factor
matrices; omitted matrices, or \code{apply_assessment_factors = FALSE}, are
treated as unit multipliers.}
}
\value{
Matrix of simulated NOEC draws with species in rows and iterations in columns.
}
\description{
Samples NOEC distributions per species using triangular, trapezoidal, or
stepwise sampling depending on available endpoints. This is the original
PSSD+ formulation; prefer \code{\link{do.pSSD_mod}} for new analyses that
include alignment-derived standard deviations.
}
\details{
See \code{\link{do.pSSD_mod}} for a note on the dot-notation naming
convention.
}
\examples{
\dontrun{
  DP  <- matrix(c(10, 20, 40), nrow = 3, ncol = 1,
                dimnames = list(NULL, "Daphnia_magna"))
  result <- do.pSSD(
    DP = DP,
    SIM = 100, CV.DP = 0.3, CV.UF = 0.5,
    apply_assessment_factors = FALSE
  )
}
}
