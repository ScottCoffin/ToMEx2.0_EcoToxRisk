% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssd_functions.R
\name{plotly_ssd}
\alias{plotly_ssd}
\title{Interactive plotly SSD for tiered_SSD objects}
\usage{
plotly_ssd(
  object,
  x_label = "Concentration",
  erm_label = NULL,
  lower_size = NULL,
  upper_size = NULL
)
}
\arguments{
\item{object}{A \code{tiered_SSD} object returned by \code{\link[=make_tiered_SSDs]{make_tiered_SSDs()}}.}

\item{x_label}{Character string for the x-axis and hover label (dose metric
and units, e.g. \code{"particles/L"}). Default \code{"Concentration"}.}

\item{erm_label}{Optional ERM name shown in the plot title (e.g.
\code{"Food Dilution"}). Default \code{NULL}.}

\item{lower_size}{Optional lower particle-size limit (um) for the x-axis
title (e.g. \code{1}). Only used when \code{upper_size} is also supplied.}

\item{upper_size}{Optional upper particle-size limit (um) for the x-axis
title (e.g. \code{5000}). Formatted with comma separator.}
}
\value{
A \code{plotly} htmlwidget. Returns \code{NULL} (with a warning) when fewer
than 6 species were available.
}
\description{
Produces an interactive Plotly version of the Species Sensitivity
Distribution plot. The layout mirrors \code{\link[=autoplot.tiered_SSD]{autoplot.tiered_SSD()}} but uses a
log-scale x-axis with hover tooltips and an arrow annotation that points
exactly to the HCx position on the SSD curve.
}
\details{
Requires the \code{plotly} package (\code{install.packages("plotly")}).
}
\examples{
\dontrun{
result <- make_tiered_SSDs(fd_water, tier = 3)
plotly_ssd(result, x_label = "particles/L", erm_label = "Food Dilution")
plotly_ssd(result, x_label = "particles/L",
           lower_size = 1, upper_size = 5000)
}

}
\seealso{
\code{\link[=autoplot.tiered_SSD]{autoplot.tiered_SSD()}} for the static ggplot2 version,
\code{\link[=make_tiered_SSDs]{make_tiered_SSDs()}}
}
