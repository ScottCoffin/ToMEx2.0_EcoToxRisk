% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/do_pssd.R
\name{do.pSSD_mod}
\alias{do.pSSD_mod}
\title{Generate probabilistic species sensitivity distributions (PSSD++)}
\usage{
\method{do}{pSSD_mod}(.data = NULL, ...)
}
\arguments{
\item{.data}{Matrix of endpoint values (rows = endpoints, cols = species). When
provided, it is treated as \code{DP}.}

\item{...}{Additional arguments used by the sampler. Expected arguments include
\code{DP}, \code{DP.SD}, \code{UFt}, \code{UFdd}, \code{SIM}, \code{CV.DP}, \code{CV.UF}, \code{rmore_method},
and \code{apply_assessment_factors}. \code{UFt} and \code{UFdd} are assessment-factor
matrices for exposure-time and endpoint-to-NOEC conversion, respectively.
They are optional; when omitted, or when \code{apply_assessment_factors = FALSE},
unit matrices are used and endpoints are not divided by assessment factors.}
}
\value{
Matrix of simulated NOEC draws with species in rows and iterations in columns.
}
\description{
Propagates intra- and inter-laboratory uncertainty to sample NOEC
distributions per species with either stepwise or log-normal
bootstrapping across endpoints.
}
\details{
\strong{Naming convention:} The dot in \code{do.pSSD_mod} and \code{do.pSSD} follows R's
S3 dispatch notation, where a function named \code{generic.class} is the method
for class \code{class}. Here the dot is part of the legacy name inherited from
Wigger et al. (2020) and does \strong{not} indicate S3 dispatch — no object of
class \code{pSSD} is defined. New code should call these functions by their full
names rather than relying on dispatch.
}
\examples{
\dontrun{
  # Minimal single-species example
  DP   <- matrix(c(10, 20, 40), nrow = 3, ncol = 1,
                 dimnames = list(NULL, "Daphnia_magna"))
  DP.SD <- DP * 0.1
  result <- do.pSSD_mod(
    DP = DP, DP.SD = DP.SD,
    SIM = 100, CV.DP = 0.3, CV.UF = 0.5, rmore_method = "step",
    apply_assessment_factors = FALSE
  )
}
}
