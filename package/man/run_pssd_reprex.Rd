% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_reprex.R
\name{run_pssd_reprex}
\alias{run_pssd_reprex}
\title{Run the PSSD++ reproducible example}
\usage{
run_pssd_reprex(
  data_path = NULL,
  cache_dir = file.path(tempdir(), "pssd_cache"),
  figure_dir = file.path(tempdir(), "pssd_figures"),
  sim = 10,
  n_sim = 10,
  workers = max(1, parallel::detectCores() - 1),
  overwrite_cache = FALSE,
  parallel = TRUE,
  seed = NULL,
  quantile_type = 8,
  cv_uf = 0.5,
  apply_assessment_factors = TRUE,
  rmore_method = "step"
)
}
\arguments{
\item{data_path}{Path to the input dataset. Defaults to the bundled
\code{aoc_z_tomex2.RDS}.}

\item{cache_dir}{Directory for cached PSSD objects.}

\item{figure_dir}{Directory for saved figure outputs.}

\item{sim}{Number of PSSD simulations per combination.}

\item{n_sim}{Number of Monte Carlo alignment simulations.}

\item{workers}{Number of workers for both alignment and PSSD phases.}

\item{overwrite_cache}{Logical; force recalculation even if cache exists.}

\item{parallel}{Logical; run PSSD combinations in parallel.}

\item{seed}{Optional integer seed for reproducibility.}

\item{quantile_type}{Quantile type used when summarizing outputs.}

\item{cv_uf}{Coefficient of variation for uncertainty factors.}

\item{apply_assessment_factors}{Logical; when \code{TRUE} (default), the ToMEx
assessment-factor columns \code{af.time} and \code{af.noec} are used in the PSSD++
correction. When \code{FALSE}, they are treated as unit multipliers for
sensitivity checks or NOEC-equivalent inputs.}

\item{rmore_method}{Distribution method for sampling species-level NOEC
distributions in the PSSD++ step. \code{"step"} (default) uses a trapezoidal
piecewise-uniform approximation following Wigger et al. (2020), which is
consistent with published results. \code{"lognormal"} uses a log-normal
short-cut that is faster but assumes symmetric log-scale variability; use
only when speed matters more than strict comparability to the manuscript.}
}
\value{
A list containing the aligned data, PSSD outputs, and summary table.
}
\description{
Executes the full Monte Carlo alignment and PSSD++ workflow bundled with the
repository, using the included ToMEx 2.0 ecotoxicity dataset.
}
\examples{
\dontrun{
  # Fastest possible run using the bundled mini dataset (seconds)
  run_pssd_reprex(sim = 1, n_sim = 1, workers = 1, overwrite_cache = TRUE)

  # Reproducible run with a fixed seed
  result <- run_pssd_reprex(sim = 2, n_sim = 2, workers = 1, seed = 42)
  result$pnec_summary

  # Use lognormal method (faster, less precise)
  run_pssd_reprex(sim = 2, n_sim = 2, workers = 1, rmore_method = "lognormal")
}
}
