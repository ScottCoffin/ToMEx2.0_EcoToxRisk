% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssd_functions.R
\name{make_tiered_SSDs}
\alias{make_tiered_SSDs}
\title{Fit a tiered SSD with the tier supplied as a parameter}
\usage{
make_tiered_SSDs(filtered.data, tier, hcx = NULL, nboot = 10)
}
\arguments{
\item{filtered.data}{A data frame of aligned toxicity data. Must contain
\code{dose_new}, \code{Species}, \code{Group}. Tiers 3-4 additionally require \code{bio_f}
and \code{risk.13}.}

\item{tier}{Integer 1-4 specifying the tier to apply.}

\item{hcx}{Integer percentile for the hazard concentration. Defaults to 5
(HC5) for tiers 1-3 and 10 (HC10) for tier 4.}

\item{nboot}{Number of bootstrap iterations for confidence intervals.
Default \code{10}; use 1000+ for publication results.}
}
\value{
An object of class \code{"tiered_SSD"} (a list) containing:
\describe{
\item{hc5lcl}{(Tier 1 only) HC5 lower confidence limit.}
\item{hcx_est, hcx05cl, hcx95cl}{(Tiers 2-4) HC5/HC10 estimate and 95\% CI.}
\item{n_species, n_groups, n_datapoints}{Sample-size metadata.}
\item{tier, hcx, nboot}{Parameters used.}
\item{predictions}{Full 100-row \code{ssdtools::predict()} output (one row per
percent 1-100). Serves as \code{aoc_pred} for plotting.}
\item{collapsed_data}{Species-level collapsed data frame with \code{Conc},
\code{frac} (Hazen plotting position), \code{Species}, and \code{Group} columns.
Serves as \code{aoc_ssd} for plotting.}
\item{hc_data}{Single-row subset of \code{predictions} at the chosen HCx
percentile with an added \code{est_format} column (scientific notation).
Serves as \code{aoc_hc} for annotation.}
\item{fit_dists}{The \code{ssdtools} distribution-fit object. Useful for
re-predicting with different settings.}
}
All plot-data fields are \code{NULL} when fewer than 6 species remain after
tier-specific filtering.
}
\description{
A unified interface to the tier-specific SSD functions. Data collapsing,
filtering, and HCx selection all follow the tier definitions from
\href{https://link.springer.com/article/10.1186/s43591-022-00033-3}{Mehinto et al. (2022)}
and Coffin et al. (2026):
}
\details{
\tabular{llll}{
   Tier \tab Data filter \tab Collapse \tab Returns \cr
   1 \tab All endpoints \tab 25th \%ile \tab HC5 lower CL \cr
   2 \tab All endpoints \tab 25th \%ile \tab HC5 est + 95\% CI \cr
   3 \tab Organism/Population, risk.13 != 1 \tab Median \tab HC5 est + 95\% CI \cr
   4 \tab Organism/Population, risk.13 != 1 \tab Median \tab HC10 est + 95\% CI \cr
}


The return value is an S3 object of class \code{"tiered_SSD"}. In addition to the
scalar HCx summaries it carries all data needed to reproduce the SSD plot via
\code{ggplot2::autoplot()}. The internal per-tier helpers (\code{SSD_function_t1},
\code{SSD_function_t2_3}, \code{SSD_function_t3_4}) are preserved in the source for
backward compatibility with existing scripts but are no longer exported.

\code{make_tiered_SSDs()} does not apply assessment factors internally. The input
must already contain the threshold dose in \code{dose_new}. The examples divide
aligned exposure metrics by \code{af.time * af.noec} to recreate the
Mehinto et al. (2022) threshold preparation, where \code{af.time} converts acute
values to chronic equivalents and \code{af.noec} converts LOEC/ECx/LCx values to
NOEC-equivalent values. Omit that division when your input is already
NOEC-equivalent or when intentionally running an unadjusted sensitivity
analysis.
}
\examples{
\dontrun{
aligned <- align_data(tomex2)
fd_water <- aligned |>
  dplyr::filter(
    environment \%in\% c("Freshwater", "Marine"),
    ingestible != "not ingestible",
    EC_env_v.particles.mL_ingest > 0, Group != "Algae",
    shape_f != "Not Reported", poly_f != "Not Reported"
  ) |>
  dplyr::mutate(
    dose_new = (EC_env_v.particles.mL_ingest / (af.time * af.noec)) * 1000
  ) |>
  tidyr::drop_na(dose_new) |>
  dplyr::filter(dose_new > 0)

result <- make_tiered_SSDs(fd_water, tier = 3)
print(result)
ggplot2::autoplot(result, x_label = "particles/L", erm_label = "Food Dilution")
}

}
\seealso{
\code{\link[=align_data]{align_data()}}, \code{\link[=autoplot.tiered_SSD]{autoplot.tiered_SSD()}}, \code{\link[=print.tiered_SSD]{print.tiered_SSD()}}
}
